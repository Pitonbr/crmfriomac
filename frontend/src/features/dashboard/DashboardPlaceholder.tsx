/**
 * Placeholder do Dashboard — Sprint 4 implementa KPIs, FunnelChart, RevenueChart etc.
 */
export function DashboardPlaceholder() {
  return (
    <div className="empty-state" style={{ padding: 60, textAlign: 'center' }}>
      <div style={{ fontSize: '3rem', marginBottom: 16 }} aria-hidden="true">
        📊
      </div>
      <h2 style={{ marginBottom: 8 }}>Dashboard</h2>
      <p style={{ color: 'var(--text-2)' }}>
        Em construção — KPIs, funil e métricas chegam no Sprint 4.
      </p>
    </div>
  );
}
